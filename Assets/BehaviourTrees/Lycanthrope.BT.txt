tree("Root")
	fallback
		not IsThereCitizen
		HasDiedOfOtherReasons
		not IsThereEvent
		fallback
			while 
				IsWaitingForFullMoon
				tree("FullMoonActions")
			while 
				not HasTargetCity
				GetTargetCity
			tree("Move")

tree("FullMoonActions")
	sequence
		IsFullMoon
		Attack

tree("Move")
	fallback
		HasArrivedAtTargetHextile
		MoveToNextTile