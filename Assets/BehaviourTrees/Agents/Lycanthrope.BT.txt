tree("Root")
	fallback
		HasDiedOfOtherReasons
		fallback
			while 
				IsWaitingForFullMoon
				tree("FullMoonActions")
			while 
				not HasTargetCity
				GetTargetCity
			tree("Move")

tree("FullMoonActions")
	sequence
		IsFullMoon
		Attack

tree("Move")
	fallback
		HasArrivedAtTargetHextile
		MoveToNextTile