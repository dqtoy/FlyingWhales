tree("Root")
	fallback
		not IsThereCitizen
		HasDiedOfOtherReasons
		not IsThereEvent
		sequence
			GetNextTargetTile
			tree("Move")

tree("Move")
	sequence
		MoveToNextTile
		HasArrivedAtTargetHextile
		GetNextTargetTile